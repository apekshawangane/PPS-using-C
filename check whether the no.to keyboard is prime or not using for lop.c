#include<stdio.h>
int main()
{
    int number,i rem 
    printf ("Enter the number \t");
    scanf ("%d",& num);
    for (i = 2; i < num; i++)
    {
       rem = num % i;
       if (rem = = 0)
       {
          check =1
       }
    }
           if ( check = = 0)
           printf ("prime");
        }
        else
        {
            printf (" NOT prime")
        }
        returen 0;
          
}