#include<stdio.h>

int main()
{
    int i,j,flag,rem;
    for (i =1;i < = 50; i ++)
    {
    flag=0;
    for (i=2; i < i,i++)
    {
    rem = i%i ;
    if (rem = = 0)
        {
         flag = 1; 
        }
    }
    if (flag = = 0)
      {
        printf("%d," i);
      }
    }
     return 0 ;  
}